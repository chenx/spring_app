# Copy this file to .env or .env.local.
# URL of the Spring Boot backend the BFF (Next.js API routes) proxies to.
# Only read server-side (route handlers / middleware) — never exposed to the browser.
BACKEND_URL=http://localhost:8080
ALLOWED_DEV_ORIGINS=http://localhost:3000,http://localhost:5173